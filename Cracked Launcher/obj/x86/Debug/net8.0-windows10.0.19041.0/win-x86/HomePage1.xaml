<Page
    x:Class="Cracked_Launcher.HomePage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cracked_Launcher"
    Background="Transparent">

    <Grid Background="Transparent" Padding="20">
        <StackPanel>
            <TextBlock Text="Latest News" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListView ItemsSource="{Binding NewsItems}">
                <ListView.ItemTemplate>
                    <DataTemplate                            >
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Description}" TextWrapping="WrapWholeWords" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding Date}" FontStyle="Italic" FontSize="12"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>

