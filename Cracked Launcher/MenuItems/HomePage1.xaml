<Page
    x:Class="Cracked_Launcher.HomePage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cracked_Launcher"
    Background="Transparent">

    <Grid Background="Transparent" Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Dashboard" FontSize="30" FontWeight="Bold" Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,20"/>

        <!-- Left side - Example of a dashboard item -->
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,20,0">
            <Border Background="Transparent" Width="300" Height="200" Margin="0,0,0,20">
                <TextBlock Text="Item 1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <Border Background="Transparent" Width="300" Height="200">
                <TextBlock Text="Item 2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
        </StackPanel>

        <!-- Right side - News List -->
        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock Text="Latest News" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <ListView ItemsSource="{Binding NewsItems}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:NewsItem">
                        <Border Background="Transparent" Margin="0,0,0,10" Padding="10" CornerRadius="8">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="WrapWholeWords" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding Date}" FontStyle="Italic" FontSize="12"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
